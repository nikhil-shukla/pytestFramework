version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - sudo dpkg -i allure_2.24.0-1_all.deb
      - python3 --version
      - python3 -m venv venv
      - . venv/bin/activate
      - pip3 install -r requirements.txt
  
  build:
    commands:
      - python3 -m pytest --browser=chrome-headless --env=qa --junitxml=pytest_reports/pytest_report.xml -n auto
  
  post_build:
    commands:
      - allure serve allure-results

reports:
  pytest_reports:
    files:
      - pytest_report.xml
    base-directory: pytest_reports/
    file-format: JUNITXML

artifacts:
  files:
    - pytest_reports/pytest_report.xml
    - allure-report/
